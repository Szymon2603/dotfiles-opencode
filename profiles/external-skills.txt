# Zewnętrzne skille instalowane przez: npx skills add <nazwa> --yes --global
# Jedna nazwa per linia. Komentarze przez #.
# Uruchamiane przez bootstrap.sh

# playwright — automatyzacja przeglądarki, E2E testy
# playwright

# git-master — atomic commits, rebase strategies
# git-master
